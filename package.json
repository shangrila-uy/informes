{
  "name": "shangrila-informes",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx -y sirv-cli . --port 3000 --host 0.0.0.0 --dev",
    "start": "npx -y sirv-cli . --port 3000 --host 0.0.0.0",
    "build": "mkdir -p dist && cp -r index.html src dist/"
  },
  "dependencies": {},
  "devDependencies": {}
}
